1. Clone code from github
2. Install
   Run:
   npm i
   npm i cypress
3. Start cypress
   - Default open:
     npx cypress open
     or
     npm run cy:open

- Start with selected env
  npm run start:qa
  or
  npx cypress open --env configFile=qa
