{
  "name": "nhom1-kiemthuphanmem-hutech",
  "version": "1.0.0",
  "description": "Kiem thu phan mem - Nhom 1 - HUTECH",
  "scripts": {
    "cypress:open": "cypress open",
    "qa": "cross-env ENV=qa npx cypress open",
    "cypress:run:qa": "cross-env ENV=qa npx cypress run --reporter cypress-mochawesome-reporter",
    "cypress:run:uat": "cross-env ENV=uat npx cypress run --reporter cypress-mochawesome-reporter",
    "cypress:run:prod": "cross-env ENV=prod npx cypress run --reporter cypress-mochawesome-reporter",
    "allure:generate": "allure generate allure-results --clean -o allure-report",
    "allure:open": "allure open allure-report"
  },
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.41.2",
    "@types/mocha": "^10.0.10",
    "@types/node": "^24.10.1",
    "allure-commandline": "^2.34.1",
    "cross-env": "^10.1.0",
    "cypress": "^15.6.0",
    "cypress-mochawesome-reporter": "^4.0.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
